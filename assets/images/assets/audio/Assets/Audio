<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>The Song Shack — Music</title>
  <meta name="description" content="The Song Shack — listen to our latest tracks." />
  <style>
    :root{
      --bg:#0a0f1a; --text:#eef2ff; --muted:#c8cde1;
      --gold:#ffcc66; --card:#121935; --border:#263157;
    }
    *{box-sizing:border-box}
    body{margin:0;font-family:Inter,system-ui,-apple-system,Segoe UI,Roboto,Arial,sans-serif;background:var(--bg);color:var(--text)}
    .wrap{max-width:1000px;margin:0 auto;padding:20px}
    header{display:flex;align-items:center;gap:12px;padding:14px 0;border-bottom:1px solid var(--border)}
    .title{font-weight:800;font-size:1.2rem}
    .btn{margin-left:auto;background:var(--gold);color:#0b0f1c;border:0;padding:10px 14px;border-radius:10px;font-weight:700;text-decoration:none}
    h1{font-size:2rem;margin:18px 0 8px}
    p.muted{color:var(--muted);margin:0 0 20px}
    .card{background:var(--card);border:1px solid var(--border);border-radius:14px;padding:16px;margin:14px 0}
    audio{width:100%}
    footer{border-top:1px solid var(--border);color:#99a1bd;text-align:center;padding:20px 0;margin-top:28px}
  </style>
</head>
<body>
  <div class="wrap">
    <header>
      <div class="title">The Song Shack — Music</div>
      <a class="btn" href="mailto:missyshack4@gmail.com?subject=Book%20Coaching">Book Coaching</a>
    </header>

    <h1>Featured Tracks</h1>
    <p class="muted">Click play to listen. (This page embeds audio directly using Base64 so you don’t need separate file uploads.)</p>

    <div class="card">
      <h3>Hook, Line and Time</h3>
      <audio controls preload="metadata">
        <!-- Replace ONLY the XXXXX with the full Base64 from HookLineandTime.m4a_base64.txt -->
        <source src="data:audio/mp4;base64,XXXXX_HOOK_LINE_AND_TIME_BASE64_GOES_HERE_XXXXX" type="audio/mp4">
        Your browser doesn’t support the audio element.
      </audio>
    </div>

    <div class="card">
      <h3>Talk Nice (Edit)</h3>
      <audio controls preload="metadata">
        <!-- Replace ONLY the YYYYY with the full Base64 from TalkNiceEdit.mp3_base64.txt -->
        <source src="data:audio/mpeg;base64,YYYYY_TALK_NICE_EDIT_BASE64_GOES_HERE_YYYYY" type="audio/mpeg">
        Your browser doesn’t support the audio element.
      </audio>
    </div>

    <footer>
      © <span id="y"></span> The Song Shack — SongShackOnTheGo
    </footer>
  </div>

  <script>document.getElementById('y').textContent = new Date().getFullYear();</script>
</body>
</html>
